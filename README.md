# Progress Campus Wayfinder

## Files

- `index.html` — complete standalone application. It includes the HTML, CSS, JavaScript, campus routing data, room coordinates, and floor-plan images.

## Run locally

```bash
python3 -m http.server 8080
```

Open http://localhost:8080/ in a browser. Localhost or HTTPS is required for browser GPS permission.
